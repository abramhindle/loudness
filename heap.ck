/* not a heap */
[ s, s, s] => Object arr[];

class Heap
{
    int capacity;
    // overload me!
    fun float f( int index ) {
        return 0.0;
    }
    //capacity
    fun int cap() {
        return capacity;
    }
    fun void add( float value, 
}
